# AX1 Bizplan 기여 안내

팀원이 문제나 아이디어를 에이전트와 익명화한 뒤 성숙도에 따라 Discussion·Issue·PR 중 적절한 창구로 제안하고, 구현이 결정되면 자신의 Fork에서 수정·검증한 PR을 원본 저장소에 제출함. 배포자는 최신 `main`에 맞게 통합·검증·버전 결정·릴리즈하며 Google Form은 개선 접수에 사용하지 않음

## 에이전트의 제안 채널 선택

| 상태 | 선택 | 판단 기준 |
|---|---|---|
| 아이디어만 있고 목표·범위·대안이 열려 있음 | Discussion | 여러 사람의 의견과 관리자 방향 결정이 먼저 필요함 |
| 문제·요구사항·완료조건은 비교적 명확하지만 아직 구현하지 않음 | Issue | 재현, 요구사항, 담당·우선순위와 구현 결정을 추적할 수 있음 |
| 구현 범위가 확정되고 수정·테스트·변경 조각이 준비됨 | Pull Request | 원본 저장소에 병합 가능한 변경을 제안함 |

에이전트는 사용자의 설명을 보고 선택한 채널과 이유를 먼저 알림. Discussion 기능을 사용할 수 없으면 Issue로 등록하고 `논의 필요`와 관리자가 결정해야 할 항목을 적음. 민감정보 없는 내용만 등록하며 GitHub 인증이나 외부 등록 권한이 없으면 반복 시도하지 않고 필요한 조치를 알려줌

아이디어는 다음 흐름으로 발전시킴

```text
아이디어 제안
→ Discussion 또는 Issue 등록
→ 프로젝트 관리자와 방향 협의
→ 구현 결정
→ 코드·문서 수정 및 커밋
→ PR 제출
```

## 주간 개선·배포 주기

- 팀원은 개선사항이 생길 때마다 기다리지 않고 성숙도에 맞는 Discussion·Issue·PR을 등록함
- 배포자는 매주 일요일까지 들어온 PR을 최신 `main` 기준으로 취합·검토하고, 안전한 변경만 통합·검증함
- 검증을 통과한 묶음은 매주 월요일 새 Release로 배포함. 추가 확인이나 수정이 필요한 PR은 다음 주기로 넘기고 이유를 PR에 기록함
- 반영할 검증 완료 변경이 없으면 빈 버전·태그를 만들지 않고 현재 안정 Release를 유지함
- Release의 `CHANGELOG`와 릴리즈 노트에는 PR 번호, 기여자가 선택한 표시명 또는 GitHub 계정, 실제 반영 내용, 제외·보류 범위와 검증 상태를 기록함

## 기여자 작업 범위

- 한 PR에는 한 가지 목적의 변경만 담음
- 최신 `main`에서 `contrib/<짧은-주제>` 브랜치를 만들어 작업함
- `VERSION`, `.codex-plugin/plugin.json`, 릴리즈용 `CHANGELOG.md` 섹션, 태그와 GitHub Release는 수정하지 않음
- 사용자에게 보이는 변경은 `.changes/<짧은-주제>.md`에 기록함
- 공통 확인 게이트, 산출물 버전·최신본 연동 규칙과 설치 정책을 임의로 제거하지 않음
- 스킬 설치를 저장소 링크·정션에 의존하게 만들지 않음. 공개 통합 ZIP과 CI 내부 개별 스킬 ZIP 격리 검사에서 같은 기능이 동작해야 함

## 변경 조각 형식

`.changes/<짧은-주제>.md`에는 다음 항목을 작성함

```markdown
# 변경 제목

- 사용자 효과: 무엇이 달라지는지
- 변경 범위: 수정한 스킬·문서·스크립트
- 제외 범위: 이번 PR에서 하지 않은 것
- 검증: 실행한 검사와 결과
- 호환성: 기존 사용 흐름 영향 또는 없음
- 기여자: 이름 또는 GitHub 계정
```

`기여자`는 릴리즈 노트에 표시할 이름을 적음. 개인 이메일·전화번호와 내부 계정정보는 넣지 않음

## 필수 검증

```powershell
python scripts/build_release.py
```

변경한 스크립트의 직접 테스트도 수행함. HWPX 변경은 익명 샘플로 CRC·XML·open-safety·readback을 확인하고, 시각 검증을 하지 못했으면 `한컴 시각 검증 대기`라고 적음

## 문서·개인정보

- 실제 공고문, RFP, 채워진 계획서, 과제번호, 성명, 이메일, 전화번호, 계정정보를 커밋하지 않음
- 양식이 꼭 필요하면 자리표시자로 익명화하고 배포 권한·출처·SHA-256·검증 방법을 PR에 기록함
- 체크박스처럼 의미가 있는 기호는 다른 문자로 자동 치환하지 않음

## Fork 기반 PR 생성

원본 저장소에 직접 쓰기 권한이 없어도 다음 표준 흐름으로 자유롭게 PR을 제출할 수 있음

```text
원본 저장소
  ↓ Fork
내 GitHub 저장소
  ↓ Clone
로컬 저장소
  ↓ 작업·Commit·Push
내 GitHub 저장소의 작업 브랜치
  ↓ Pull Request
원본 저장소
```

1. `RealMyeong/ax1-bizplan`을 자신의 GitHub 계정으로 Fork함
2. 자신의 Fork를 로컬에 Clone하고 원본 저장소를 `upstream` 원격으로 등록함
3. 최신 `upstream/main`에서 `contrib/<짧은-주제>` 브랜치를 만듦
4. 한 가지 목적의 변경과 `.changes`를 작성하고 직접 테스트와 전체 빌드를 실행함
5. 커밋을 자신의 Fork 작업 브랜치에 Push함
6. 자신의 작업 브랜치를 비교 브랜치, 원본 저장소 `main`을 기준 브랜치로 PR을 생성함

Fork의 `main`은 원본 `main`을 동기화하는 용도로 유지하고 작업 커밋을 직접 쌓지 않음. 에이전트는 `origin`이 팀원의 Fork인지, `upstream`이 원본 저장소인지 확인한 뒤 Push와 PR 대상을 사용자에게 설명함

## 에이전트 요청 예시

아이디어·문제·구현 수준을 에이전트가 먼저 판단하게 하려면 다음처럼 요청함

> 아래 개선 내용을 민감정보 없이 정리하고 Discussion, Issue, PR 중 적절한 창구를 선택해줘. 선택 이유를 먼저 설명하고, 아이디어 단계면 토론 또는 추적 항목만 등록해 관리자와 협의할 질문을 남겨줘. 구현 범위가 확정된 경우에만 내 Fork를 Clone하고 `upstream/main` 기준 `contrib/<주제>` 브랜치에서 수정·검증·커밋·Push한 뒤 원본 저장소로 PR을 만들어줘.

PR 본문은 저장소 양식을 모두 채움. 에이전트에게 요청할 때는 다음처럼 말할 수 있음

> 이 저장소의 AGENTS.md와 CONTRIBUTING.md를 읽고, 현재 변경을 검사한 뒤 AX1 PR 운영 원칙에 맞는 브랜치·커밋·PR을 만들어줘. 버전과 릴리즈 파일은 수정하지 말고 변경 조각을 추가해줘.

구현이 결정되어 바로 수정할 수 있을 때는 다음처럼 전 과정을 요청할 수 있음

> 원본 `RealMyeong/ax1-bizplan`과 내 Fork의 원격 구성을 확인하고 최신 `upstream/main`에서 `contrib/<주제>` 브랜치를 만들어줘. 아래 문제를 민감정보 없는 예시로 재현한 뒤 한 가지 목적의 변경으로 수정해줘. `.changes`를 추가하고 직접 테스트와 `python scripts/build_release.py`를 실행해줘. 통과하면 내 Fork에 커밋·Push하고 원본 저장소로 PR을 생성한 다음 PR 링크와 미검증 항목을 알려줘. `VERSION`, 릴리즈용 `CHANGELOG.md`, 플러그인 버전과 태그는 수정하지 마.

PR 제출에는 GitHub 인증과 저장소 쓰기 권한 또는 fork 권한이 필요함. 권한이 없으면 에이전트는 반복 시도하지 않고 필요한 권한을 알려줘야 함

배포자는 PR을 그대로 합칠 수도 있고 최신 공통 정책에 맞게 일부를 이식할 수도 있음. 기여자의 원 커밋·공동 작성자 표기는 가능한 범위에서 보존함
