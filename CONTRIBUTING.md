# AX1 Bizplan 기여 안내

팀원이 문제나 아이디어를 에이전트와 익명 재현하고 로컬에서 스킬을 개선한 뒤 PR로 제안하며, 배포자가 최신 `main`에 맞게 통합·검증·버전 결정·릴리즈하는 방식으로 운영함. Google Form은 개선 접수에 사용하지 않음

## 주간 개선·배포 주기

- 팀원은 개선사항이 생길 때마다 기다리지 않고 PR을 생성함
- 배포자는 매주 일요일까지 들어온 PR을 최신 `main` 기준으로 취합·검토하고, 안전한 변경만 통합·검증함
- 검증을 통과한 묶음은 매주 월요일 새 Release로 배포함. 추가 확인이나 수정이 필요한 PR은 다음 주기로 넘기고 이유를 PR에 기록함
- 반영할 검증 완료 변경이 없으면 빈 버전·태그를 만들지 않고 현재 안정 Release를 유지함
- Release의 `CHANGELOG`와 릴리즈 노트에는 PR 번호, 기여자가 선택한 표시명 또는 GitHub 계정, 실제 반영 내용, 제외·보류 범위와 검증 상태를 기록함

## 기여자 작업 범위

- 한 PR에는 한 가지 목적의 변경만 담음
- 최신 `main`에서 `contrib/<짧은-주제>` 브랜치를 만들어 작업함
- `VERSION`, `.codex-plugin/plugin.json`, 릴리즈용 `CHANGELOG.md` 섹션, 태그와 GitHub Release는 수정하지 않음
- 사용자에게 보이는 변경은 `.changes/<짧은-주제>.md`에 기록함
- 공통 확인 게이트, 산출물 버전·최신본 연동 규칙과 설치 정책을 임의로 제거하지 않음
- 스킬 설치를 저장소 링크·정션에 의존하게 만들지 않음. 개별 스킬 ZIP과 전체 배포 ZIP에서 같은 기능이 동작해야 함

## 변경 조각 형식

`.changes/<짧은-주제>.md`에는 다음 항목을 작성함

```markdown
# 변경 제목

- 사용자 효과: 무엇이 달라지는지
- 변경 범위: 수정한 스킬·문서·스크립트
- 제외 범위: 이번 PR에서 하지 않은 것
- 검증: 실행한 검사와 결과
- 호환성: 기존 사용 흐름 영향 또는 없음
- 기여자: 이름 또는 GitHub 계정
```

`기여자`는 릴리즈 노트에 표시할 이름을 적음. 개인 이메일·전화번호와 내부 계정정보는 넣지 않음

## 필수 검증

```powershell
python scripts/build_release.py
```

변경한 스크립트의 직접 테스트도 수행함. HWPX 변경은 익명 샘플로 CRC·XML·open-safety·readback을 확인하고, 시각 검증을 하지 못했으면 `한컴 시각 검증 대기`라고 적음

## 문서·개인정보

- 실제 공고문, RFP, 채워진 계획서, 과제번호, 성명, 이메일, 전화번호, 계정정보를 커밋하지 않음
- 양식이 꼭 필요하면 자리표시자로 익명화하고 배포 권한·출처·SHA-256·검증 방법을 PR에 기록함
- 체크박스처럼 의미가 있는 기호는 다른 문자로 자동 치환하지 않음

## PR 생성

PR 본문은 저장소 양식을 모두 채움. 에이전트에게 요청할 때는 다음처럼 말할 수 있음

> 이 저장소의 AGENTS.md와 CONTRIBUTING.md를 읽고, 현재 변경을 검사한 뒤 AX1 PR 운영 원칙에 맞는 브랜치·커밋·PR을 만들어줘. 버전과 릴리즈 파일은 수정하지 말고 변경 조각을 추가해줘.

개선 아이디어만 있고 아직 수정하지 않았을 때도 다음처럼 에이전트에게 전 과정을 요청할 수 있음

> 최신 main에서 `contrib/<주제>` 브랜치를 만들고, 아래 문제를 민감정보 없는 예시로 재현한 뒤 한 가지 목적의 변경으로 수정해줘. `.changes`를 추가하고 직접 테스트와 `python scripts/build_release.py`를 실행해줘. 통과하면 커밋·푸시하고 PR을 생성한 다음 PR 링크와 미검증 항목을 알려줘. `VERSION`, 릴리즈용 `CHANGELOG.md`, 플러그인 버전과 태그는 수정하지 마.

PR 제출에는 GitHub 인증과 저장소 쓰기 권한 또는 fork 권한이 필요함. 권한이 없으면 에이전트는 반복 시도하지 않고 필요한 권한을 알려줘야 함

배포자는 PR을 그대로 합칠 수도 있고 최신 공통 정책에 맞게 일부를 이식할 수도 있음. 기여자의 원 커밋·공동 작성자 표기는 가능한 범위에서 보존함
